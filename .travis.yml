language: java
jdk:
        - openjdk8

branches:
        only:
                - main

# Travis CI home of server
cache:
        directories:
                - '$HOME/.m2/repository'
                - '$HOME/.gradle'
script: "./gradlew clean build"

# Alarm by mail when CI run is complete
notifications:
        email:
                recipients:
                        - seokho7799@gmail.com
